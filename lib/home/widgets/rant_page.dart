import 'package:flutter/material.dart';
import 'write_entry_page.dart';

class RantPage extends StatelessWidget {
  const RantPage({super.key});

  @override
  Widget build(BuildContext context) => const WriteEntryPage(tag: 'rant');
}
